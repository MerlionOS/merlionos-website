---
title: MerlionOS
description: A Singapore-inspired AI-native hobby operating system written in Rust for x86_64.
template: splash
hero:
  tagline: Born for AI. Built by AI. — A hobby OS kernel written in Rust for x86_64.
  actions:
    - text: Get Started
      link: /getting-started/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/MerlionOS/merlion-kernel
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Why MerlionOS?

<CardGrid stagger>
  <Card title="Written in Rust" icon="seti:rust">
    Memory-safe kernel with zero undefined behavior. No C, no compromises.
  </Card>
  <Card title="AI-Native Architecture" icon="star">
    NL shell, neural network inference, ML training, vector store, AI workflows, and self-evolution built into the kernel.
  </Card>
  <Card title="223 Modules, 65K Lines" icon="document">
    Comprehensive kernel: memory management, multitasking, SMP, filesystems, TCP/IP networking, servers, GPU, audio, GUI, and more.
  </Card>
  <Card title="298 Shell Commands" icon="laptop">
    A rich interactive environment with process management, file operations, networking tools, and system diagnostics.
  </Card>
</CardGrid>

## At a Glance

- **Preemptive multitasking** with per-process page tables, signals, and process groups
- **SMP support** with APIC, HPET timers
- **Filesystems**: VFS, FAT16, ext2, ext4, tmpfs, procfs, sysfs
- **Storage**: virtio-blk, AHCI, NVMe, GPT partitions
- **Networking**: TCP (Reno/Cubic/BBR), UDP, IPv4+IPv6, DHCP, DNS, TLS, WebSocket, MQTT
- **Servers**: HTTP, SSH, DNS, MQTT broker, HTTPS reverse proxy
- **Security**: Capabilities, seccomp, ACLs, AES-128, RSA, X.509
- **Hardware**: PCI, USB (xHCI), Bluetooth, e1000e NIC, ACPI power management
- **Languages**: Forth, Lisp, WASM/WASI runtime, shell scripting
- **60 releases** from v1 through v60

## Quick Start

```sh
# Prerequisites: Rust nightly + QEMU
rustup component add rust-src llvm-tools --toolchain nightly
cargo install bootimage
brew install qemu  # macOS

# Build and run
git clone https://github.com/MerlionOS/merlion-kernel.git
cd merlion-kernel
make run
```
